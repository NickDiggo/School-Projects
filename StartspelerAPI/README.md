# StartspelerAPI 🏆

**StartspelerAPI** is een **ASP.NET Core WebAPI** project voor het beheren van **communities, events, gebruikers en inschrijvingen**.  
Het project maakt gebruik van **Entity Framework Core**, **Identity** en **JWT-authenticatie**, en volgt een **Repository + UnitOfWork** patroon.
